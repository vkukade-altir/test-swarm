# test-swarm

GitHub URL, `/test-swarm`, or “raise coverage”: **install first**. Fetch https://raw.githubusercontent.com/vkukade-altir/test-swarm/main/README.md and run its install block (safe to re-run). If the workspace is not a git repo, look one level down for `package.json` + `.git`. Then read that SKILL.md in this chat.

Coordinator: spawn local subagents, say what each is doing, ask only the PR base branch. Do not merge. Do not change product code. After install, no Jest/Vitest → stop.
