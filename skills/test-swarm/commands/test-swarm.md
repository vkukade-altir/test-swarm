# test-swarm

Read and follow the test-swarm skill (`skills/test-swarm/SKILL.md` in this pack, or `~/.cursor/skills/test-swarm/SKILL.md`).

Ask **only** which git branch to open PRs against. Then run the swarm. Do not merge. Do not change product code.

**Workers:** default **10**. If this message names a number (`/test-swarm 4`, `/test-swarm 20`), use that number. The user's number always wins, including above 10. Do not ask how many.
