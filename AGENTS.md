# test-swarm

Skill pack. **Install first** into the app, then checks.

If the workspace root has no `.git`, look one level down for `package.json` + `.git`. Install with the README block (it is safe to re-run). Then read that SKILL.md in this chat and run. Ask only the PR base branch. Do not merge. Do not change product code.
