# test-swarm

GitHub URL, `/test-swarm`, or “raise coverage”: **install first**. If the workspace is not a git repo, look one level down for `package.json` + `.git`. Copy `skills/test-swarm` into that app’s `.cursor/skills/test-swarm`, then read that SKILL.md in this chat.

Coordinator: spawn local subagents, say what each is doing, ask only the PR base branch. Do not merge. Do not change product code. After install, no Jest/Vitest → stop.
