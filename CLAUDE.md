# test-swarm

Paste of this GitHub URL, `/test-swarm`, or “raise coverage”: copy `skills/test-swarm` into the **app** `.cursor/skills/test-swarm` if needed, then read that SKILL.md and follow it in this chat.

Coordinator: spawn local subagents, say what each is doing, ask only the PR base branch. Do not merge. Do not change product code. No Jest/Vitest → stop.
