# test-swarm

When the user says **test-swarm**, **`/test-swarm`**, **95% coverage**, pastes https://github.com/vkukade-altir/test-swarm, or asks to raise JS/TS test coverage: install into the **app** repo if needed (copy `skills/test-swarm` to `.cursor/skills/test-swarm`, not the whole git clone), then read `.cursor/skills/test-swarm/SKILL.md` or `skills/test-swarm/SKILL.md` and follow it in this chat.

You are the coordinator. Spawn local subagents. Tell the user what each worker is doing. Ask only which git branch to open PRs against. Do not merge. Do not change product code. If there is no Jest or Vitest, stop. Default 10 workers; a number in the prompt wins.
